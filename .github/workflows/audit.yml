name: audit

on:
  schedule:
    - cron: "0 11 * * 0"

  workflow_dispatch:

jobs:
  security:
    uses: ./.github/workflows/build.yml

    permissions:
      contents: write
      security-events: write

    secrets: inherit

    with:
      scan: true
